<div class="container store-index">
	<div class="row">
		<% if @posts.blank? %>
			<p>お気に入り店の登録をすると当ページに登録店のタイムラインが表示されます。</p>
			<p>ヘッダーの店舗一覧、ランキングでお店を探して登録しましょう。</p>
		<% end %>

		<div class="col-sm-12 col-md-8">

			<div class="col-xs-12 store_send-index">
				<% @posts.each do |post| %>
					<div class="store-sends">
						<div class="store_send_top">

							<%= attachment_image_tag post.store, :store_image, format: "jpeg", size: "50x50", fallback: asset_path("no-image.jpeg"), size:'50x50' %>

							<%= post.store.corporation_name %>　<%= post.store.store_name %>

							<%= link_to "店舗詳細", users_store_path(post.store),  class: "btn btn-primary" %>

						</div>

						<div class="store_send_bottom">

							<%= attachment_image_tag post, :send_image, format: "jpeg", size: "300x250", fallback: asset_path("not-image.jpeg"), size:'300x250' %>

							<div class="store-send-bottom-insite">
								<h4><%= post.title %></h4>
								<p><%= simple_format(h(post.send_comment)) %></p>
							</div>
						</div>

							<div>
								<% post.tags.each do |tag| %>
        							<span class="label"><%= tag.name %></span>
      							<% end %>
							</div>

							<div class="store_send_create">
								<p>投稿時間:　<%= l post.created_at %></p>
							</div>
					</div>
				<% end %>
				<%= paginate @posts %>
			</div>
		</div>

	<div class="row">
		<div class="col-sm-12 col-md-4 side-bar">

			<div class="timeline-follow">
				<% @relationship_stores.each do |r_store| %>
					<div class="timeline-follow-box">

					<%= attachment_image_tag r_store, :store_image, format: "jpeg", size: "50x50", fallback: asset_path("no-image.jpeg"), size:'50x50' %>

					<%= r_store.corporation_name %>　<%= r_store.store_name %>
					<p><%= link_to "店舗詳細", users_store_path(r_store), class: "btn btn-primary" %></p>
				</div>
				<% end %>
			</div>
		</div>
	</div>



	</div>
</div>